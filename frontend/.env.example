# URL of the deployed backend (Render), no trailing slash.
# Local dev against a backend running on :10000, for example:
# VITE_API_BASE=http://localhost:10000
VITE_API_BASE=https://<your-render-service>.onrender.com
